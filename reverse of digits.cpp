#include<stdio.h>
int main()
{
	int n,num,rnum;
	printf("\n Enter a num:");
	scanf("%d",&n);
	while(num!=0);
	{
		num = num%10;
		rnum = num;	
	}
	    printf("\n reverse of digits is %d",num);
}
